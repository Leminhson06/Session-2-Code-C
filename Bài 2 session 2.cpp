#include <sdio.h>

int main() {
    // Khai báp và khởi tạo hai biến số nguyên 
    int num1 = 10; 
    //Biến 'num1' được khởi tạo với giá trị 10
    int num2 = 20; 
    //Biến 'num2' được khởi tạo với giá trị 20

    // Khai báo một biến khác để lưu kết quả tổng
    int sum = num1 + num2 //Biến 'sum' lưu kết quả của 'num1' + 'num2'

    // In kết quả tổng ra màn hình
    printf("Tổng cảu %d và %d là: %d\n", num1, num2, sum);

    return 0;
}

